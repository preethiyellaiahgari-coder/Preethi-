#include<stdio.h>
int main()
{
int a,b,remainder, quotient;
printf("Enter the two values");
scanf("%d%d",&a,&b);
quotient=a/b;
remainder=a%b;
printf("quotient=%d\n", quotient);
printf("remainder=%d\n", remainder);
  return 0;
  }
  
